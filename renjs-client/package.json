{
  "name": "spellcrafter-renjs-client",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --open",
    "build": "tsc && bunx --bun vite build",
    "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dojoengine/core": "^0.1.56",
    "renjs": "^2.9.3",
    "starknet": "^5.24.3"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.9"
  }
}