query getEntities {
  entities(keys: ["%"]) {
    edges {
      node {
        keys
        components {
          __typename
          ... on Owner {
            address
          }
        }
      }
    }
  }
}